const heros = ["mango", "apple", "banana", "angur"]

// heros.map((hero: string) => { //wrong syntax
//     return 1 
// })


// heros.map((hero: string): string => {
//     return `fruits is ${hero}`
// })

function ConsoleErr(): {name:string, phone:number}{
    return {name: "manish", phone: 9867656}
}

